'use strict'

class Poll {
  /* istanbul ignore next */
  getEvent (event) {
    return event
  }

  /* istanbul ignore next */
  handleSuccess (result) {
  }

  /* istanbul ignore next */
  handleFailure (error) {
  }
}

module.exports = Poll
